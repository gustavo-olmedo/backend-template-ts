services:
  nest-backend:
    build: .
    ports:
      - 8000:3000
    container_name: nest-backend
    volumes:
      - .:/app
    depends_on:
      - db
  db:
    image: postgres:16.8
    restart: always
    environment:
      - POSTGRES_PASSWORD=postgres
    container_name: nest-db
    volumes:
      - ./pgdata:/var/lib/postgresql/data
      - ./init_testdb.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - '5432:5432'
  pgadmin:
    image: dpage/pgadmin4
    restart: always
    container_name: nest-pgadmin4
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@admin.com
      - PGADMIN_DEFAULT_PASSWORD=pgadmin4
    ports:
      - '5050:80'
    depends_on:
      - db
